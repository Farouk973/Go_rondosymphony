<form method="post" action="{{ path('participant_delete', {'idPar': participant.idPar}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ participant.idPar) }}">
    <button class="btn">Delete</button>
</form>
